<Page
    x:Class="signedUWP.UserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:signedUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
       
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>

        <Grid x:ConnectionId='1' Name="MainGrid1" Grid.Column="0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <Image Source="Assets/WIN13.png"  Grid.Row="1" Grid.Column="1" Margin="0,0,0,150"/>
        </Grid>
        <Grid x:ConnectionId='2' Name="MainGrid2" Grid.Column="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
            </Grid.ColumnDefinitions>

            <ListView x:ConnectionId='6' Name="InventoryList"  Grid.Row="1" Grid.Column="1"        
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.IsVerticalRailEnabled="True"
                      ScrollViewer.VerticalScrollMode="Enabled"
                      ScrollViewer.HorizontalScrollMode="Enabled"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.IsHorizontalRailEnabled="True"
                      Margin="0,0,0,40"
                      CanDragItems="True"
                                                                         
                                                       
                      IsItemClickEnabled="True"                                     SelectionMode="Single">
                <ListView.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal"  Margin="0,40,0,0">
                            
                            <TextBlock Text="Name" Width="110" FontSize="18" Foreground="DarkRed"/>
                            <TextBlock Text="Arrived State" Width="140" FontSize="18" Foreground="DarkRed" />
                            <TextBlock Text="Time" Width="130" FontSize="18" Foreground="DarkRed" />
                            <TextBlock Text="Tag Id" Width="120" FontSize="18" Foreground="DarkRed" />
                        </StackPanel>

                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemTemplate>
                    <DataTemplate                         >
                        <StackPanel x:ConnectionId='7' Orientation="Horizontal" >
                            
                            <TextBlock x:ConnectionId='8' Name="UserName"
                                                            
                                        Width="110" />
                            <TextBlock x:ConnectionId='9'  Name="Arrived"
                                                               
                                       Width="110" />
                            <TextBlock x:ConnectionId='10'  Name="Updatatime"
                                                                  
                                       Width="160" />
                            <TextBlock x:ConnectionId='11'  Name="TagID"
                                                             
                                        Width="160" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
        </Grid>
        <Grid x:ConnectionId='3' Name="MainGrid3" Grid.Row="1" Grid.Column="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='4' HorizontalAlignment="Right"                     Grid.Row="2" Grid.Column="1" 
                            Background="LightGray"
                            Height="40" Width="150">
                <StackPanel>
                    <TextBlock Text="Clear" Foreground="Black"
                       HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button x:ConnectionId='5' HorizontalAlignment="Center"                        Grid.Row="2" Grid.Column="1" 
                            Background="LightGray"
                            Height="40" Width="150">
                <StackPanel>
                    <TextBlock Text="Clear All" Foreground="Black"
                       HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        </Grid>
</Page>

