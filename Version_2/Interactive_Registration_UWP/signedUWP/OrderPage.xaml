<Page
    x:Class="signedUWP.OrderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:signedUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="400" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>
        <Grid Name="MainGrid2" Grid.Column="0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <ListView Name="InventoryList"  Grid.Row="1" Grid.Column="1"        
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.IsVerticalRailEnabled="True"
                      ScrollViewer.VerticalScrollMode="Enabled"
                      ScrollViewer.HorizontalScrollMode="Enabled"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.IsHorizontalRailEnabled="True"
                      Margin="0,0,0,40"
                      CanDragItems="True"
                      DragItemsStarting="InventoryList_DragItemsStarting"
                      DragOver="InventoryList_DragOver"
                      AllowDrop ="True"
                      IsItemClickEnabled="True" ItemClick="InventoryList_ClickItem" SelectionMode="Single">
                <ListView.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal"  Margin="0,20,0,0">
                            <TextBlock Text="Id" Width="90" FontSize="20" Foreground="DarkRed"  />
                            <TextBlock Text="Name" Width="120" FontSize="20" Foreground="DarkRed"  />
                            <TextBlock Text="Company" Width="120" FontSize="20" Foreground="DarkRed"  />
                            <TextBlock Text="Arrived" Width="120" FontSize="20" Foreground="DarkRed" />
                            <TextBlock Text="VisitTime" Width="120" FontSize="20" Foreground="DarkRed" />
                            <TextBlock Text="NFC id" Width="120" FontSize="20" Foreground="DarkRed" />
                            <TextBlock Text="Tag id" Width="120" FontSize="20" Foreground="DarkRed" />
                        </StackPanel>

                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Users">
                        <StackPanel Orientation="Horizontal" >

                            <TextBlock Name="UserId"
                                        Text="{x:Bind Id}"
                                        Width="80" />
                            <TextBlock Name="UserName"
                                        Text="{x:Bind Name}"
                                        Width="120" />
                            
                            <TextBlock  Name="Company"
                                        Text="{x:Bind Company}"           
                                        Width="120" />
                            
                            <TextBlock  Name="Arrived"
                                        Text="{x:Bind Arrived}"           
                                        Width="110" />

                            <TextBlock  Name="Visittime"
                                        Text="{x:Bind UpdateTime}"
                                       Width="120" />
                            <TextBlock  Name="TagID"
                                        Text="{x:Bind NFCid}"
                                        Width="120" />
                            <TextBlock  Name="TagId"
                                        Text="{x:Bind TagId}"
                                        Width="150" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Name="MainGrid3" Grid.Column="0" Grid.Row="1"  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1" AllowDrop="True"   DragOver="Grid_DragOver" Drop="Grid_Drop" Background="LightBlue" Width="200" Height="50" >
                <TextBlock Text="Clear State" TextAlignment="Center" Margin="0,15,0,0"/>
            </Grid>
            <Grid Grid.Column="2" AllowDrop="True"   DragOver="Grid_DragOver1" Drop="Grid_Drop" Background="LightGray" Width="200" Height="50" >
                <TextBlock Text="Delete" TextAlignment="Center"  Margin="0,15,0,0"/>
            </Grid>
            <Grid Grid.Column="3" AllowDrop="True"   Background="LightBlue" Width="200" Height="50" >
                <Button Content="Delete All"  Background="LightBlue" HorizontalAlignment="Center" VerticalAlignment="Top"  Click="button_Click" Width="200" Height="50"/>
            </Grid>

        </Grid>
    </Grid>
</Page>
